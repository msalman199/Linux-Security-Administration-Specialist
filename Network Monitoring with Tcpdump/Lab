Network Monitoring with Tcpdump
Objectives
By the end of this lab, students will be able to:

Install and configure tcpdump on a Linux system
Capture network packets using various tcpdump commands and options
Apply filters to capture specific types of network traffic
Analyze captured packets to identify potential security incidents
Save captured traffic to files for later analysis
Interpret common network protocols and traffic patterns
Use tcpdump for basic network troubleshooting and security monitoring
Prerequisites
Before starting this lab, students should have:

Basic understanding of Linux command line operations
Fundamental knowledge of networking concepts (IP addresses, ports, protocols)
Familiarity with file system navigation in Linux
Basic understanding of network security concepts
Root or sudo access to perform network monitoring tasks
Lab Environment
Al Nafi provides ready-to-use Linux-based cloud machines for this lab. Simply click Start Lab to access your pre-configured environment. No need to build your own virtual machine or set up additional infrastructure. All tasks will be performed on a single Linux machine.

Task 1: Installing and Setting Up Tcpdump
Subtask 1.1: Verify Tcpdump Installation
First, check if tcpdump is already installed on your system:

which tcpdump
Check the version:

tcpdump --version
Subtask 1.2: Install Tcpdump (if needed)
If tcpdump is not installed, install it using your distribution's package manager:

For Ubuntu/Debian systems:

sudo apt update
sudo apt install tcpdump -y
For CentOS/RHEL/Fedora systems:

sudo yum install tcpdump -y
# or for newer versions
sudo dnf install tcpdump -y
Subtask 1.3: Check Network Interfaces
List all available network interfaces:

ip link show
Alternatively, use:

sudo tcpdump -D
This command shows all interfaces that tcpdump can monitor. Make note of your primary network interface (usually eth0, ens33, or enp0s3).

Task 2: Basic Packet Capture with Tcpdump
Subtask 2.1: Capture All Traffic
Start capturing all network traffic on your primary interface. Replace eth0 with your actual interface name:

sudo tcpdump -i eth0
Let this run for about 30 seconds, then stop it with Ctrl+C. You should see various packets being captured.

Subtask 2.2: Capture with Packet Count Limit
Capture only the first 10 packets:

sudo tcpdump -i eth0 -c 10
Subtask 2.3: Capture with Verbose Output
Use verbose mode to see more detailed packet information:

sudo tcpdump -i eth0 -v -c 5
For even more detail, use:

sudo tcpdump -i eth0 -vv -c 5
Subtask 2.4: Display Packet Contents
Show packet contents in both hex and ASCII:

sudo tcpdump -i eth0 -X -c 3
Show only ASCII content:

sudo tcpdump -i eth0 -A -c 3
Task 3: Applying Filters to Capture Specific Traffic
Subtask 3.1: Protocol-Based Filters
Capture only TCP traffic:

sudo tcpdump -i eth0 tcp -c 10
Capture only UDP traffic:

sudo tcpdump -i eth0 udp -c 10
Capture only ICMP traffic:

sudo tcpdump -i eth0 icmp -c 5
Subtask 3.2: Port-Based Filters
Capture HTTP traffic (port 80):

sudo tcpdump -i eth0 port 80 -c 10
Capture HTTPS traffic (port 443):

sudo tcpdump -i eth0 port 443 -c 10
Capture SSH traffic (port 22):

sudo tcpdump -i eth0 port 22 -c 10
Subtask 3.3: Host-Based Filters
Create some network activity first by generating traffic to a specific host:

# In one terminal, start tcpdump
sudo tcpdump -i eth0 host 8.8.8.8 -c 5

# In another terminal (or after stopping tcpdump), generate traffic
ping -c 3 8.8.8.8
Capture traffic from a specific source:

sudo tcpdump -i eth0 src host 8.8.8.8 -c 5
Capture traffic to a specific destination:

sudo tcpdump -i eth0 dst host 8.8.8.8 -c 5
Subtask 3.4: Complex Filters
Combine multiple conditions using logical operators:

# Capture HTTP or HTTPS traffic
sudo tcpdump -i eth0 'port 80 or port 443' -c 10

# Capture TCP traffic on specific ports
sudo tcpdump -i eth0 'tcp and (port 22 or port 80)' -c 10

# Capture traffic from specific network range
sudo tcpdump -i eth0 'net 192.168.1.0/24' -c 10
Task 4: Saving Captured Traffic to Files
Subtask 4.1: Save Packets to a File
Create a directory for storing capture files:

mkdir ~/tcpdump_captures
cd ~/tcpdump_captures
Capture packets and save to a file:

sudo tcpdump -i eth0 -w capture_all.pcap -c 50
Subtask 4.2: Save Specific Traffic Types
Capture HTTP traffic and save to file:

sudo tcpdump -i eth0 port 80 -w http_traffic.pcap -c 20
Capture ICMP traffic:

# Generate ICMP traffic first
ping -c 5 8.8.8.8 &
sudo tcpdump -i eth0 icmp -w icmp_traffic.pcap -c 10
Subtask 4.3: Read Captured Files
Read the captured files back:

tcpdump -r capture_all.pcap
Read with filters applied:

tcpdump -r capture_all.pcap tcp
Read with verbose output:

tcpdump -r http_traffic.pcap -v
Task 5: Analyzing Traffic for Security Incidents
Subtask 5.1: Generate Test Traffic
Create a script to generate various types of network traffic for analysis:

cat > generate_traffic.sh << 'EOF'
#!/bin/bash

echo "Generating test network traffic..."

# Generate HTTP requests
curl -s http://httpbin.org/get > /dev/null &

# Generate DNS queries
nslookup google.com > /dev/null &
nslookup facebook.com > /dev/null &

# Generate ICMP traffic
ping -c 3 8.8.8.8 > /dev/null &

# Generate SSH-like traffic (connection attempts)
timeout 2 telnet 127.0.0.1 22 > /dev/null 2>&1 &

echo "Traffic generation started..."
wait
echo "Traffic generation completed."
EOF

chmod +x generate_traffic.sh
Subtask 5.2: Capture and Analyze Suspicious Activity
Start capturing all traffic:

sudo tcpdump -i eth0 -w security_analysis.pcap -c 100 &
TCPDUMP_PID=$!
Run the traffic generator:

./generate_traffic.sh
Stop tcpdump:

sudo kill $TCPDUMP_PID
Subtask 5.3: Analyze the Captured Traffic
Look for different types of connections:

# Analyze all captured traffic
tcpdump -r security_analysis.pcap

# Look for DNS queries
tcpdump -r security_analysis.pcap port 53

# Look for HTTP traffic
tcpdump -r security_analysis.pcap port 80

# Look for connection attempts to SSH
tcpdump -r security_analysis.pcap port 22
Subtask 5.4: Identify Potential Security Issues
Create filters to detect common security concerns:

# Look for SYN packets (potential port scanning)
tcpdump -r security_analysis.pcap 'tcp[tcpflags] & tcp-syn != 0'

# Look for failed connections (RST packets)
tcpdump -r security_analysis.pcap 'tcp[tcpflags] & tcp-rst != 0'

# Look for large packets (potential data exfiltration)
tcpdump -r security_analysis.pcap 'greater 1000'
Task 6: Advanced Filtering and Analysis
Subtask 6.1: Time-Based Analysis
Capture traffic with timestamps:

sudo tcpdump -i eth0 -tttt -c 10
Subtask 6.2: Protocol Analysis
Create a comprehensive analysis script:

cat > analyze_traffic.sh << 'EOF'
#!/bin/bash

PCAP_FILE="$1"

if [ -z "$PCAP_FILE" ]; then
    echo "Usage: $0 <pcap_file>"
    exit 1
fi

echo "=== Traffic Analysis Report ==="
echo "File: $PCAP_FILE"
echo "Generated on: $(date)"
echo

echo "=== Total Packets ==="
tcpdump -r "$PCAP_FILE" | wc -l

echo
echo "=== Protocol Distribution ==="
echo "TCP Packets:"
tcpdump -r "$PCAP_FILE" tcp 2>/dev/null | wc -l

echo "UDP Packets:"
tcpdump -r "$PCAP_FILE" udp 2>/dev/null | wc -l

echo "ICMP Packets:"
tcpdump -r "$PCAP_FILE" icmp 2>/dev/null | wc -l

echo
echo "=== Top Destination Ports ==="
tcpdump -r "$PCAP_FILE" -n 2>/dev/null | grep -oE 'dpt:[0-9]+' | sort | uniq -c | sort -nr | head -5

echo
echo "=== Unique Hosts ==="
tcpdump -r "$PCAP_FILE" -n 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort | uniq | head -10

EOF

chmod +x analyze_traffic.sh
Run the analysis:

./analyze_traffic.sh security_analysis.pcap
Subtask 6.3: Create Security Monitoring Filters
Create a script for ongoing security monitoring:

cat > security_monitor.sh << 'EOF'
#!/bin/bash

INTERFACE="eth0"
DURATION=60

echo "Starting security monitoring on $INTERFACE for $DURATION seconds..."

# Monitor for potential port scans
echo "Monitoring for potential port scans..."
timeout $DURATION sudo tcpdump -i $INTERFACE -c 20 'tcp[tcpflags] & tcp-syn != 0 and tcp[tcpflags] & tcp-ack == 0' -w port_scan_attempts.pcap &

# Monitor for DNS queries to suspicious domains
echo "Monitoring DNS queries..."
timeout $DURATION sudo tcpdump -i $INTERFACE -c 20 'port 53' -w dns_queries.pcap &

# Monitor for large data transfers
echo "Monitoring for large packets..."
timeout $DURATION sudo tcpdump -i $INTERFACE -c 20 'greater 1500' -w large_packets.pcap &

wait

echo "Security monitoring completed. Check the generated .pcap files for analysis."
EOF

chmod +x security_monitor.sh
Task 7: Practical Security Incident Analysis
Subtask 7.1: Simulate a Security Scenario
Create a script that simulates various network activities:

cat > simulate_incident.sh << 'EOF'
#!/bin/bash

echo "Simulating network security incident..."

# Simulate normal web browsing
curl -s http://httpbin.org/user-agent > /dev/null &

# Simulate multiple connection attempts (like a brute force)
for i in {1..5}; do
    timeout 1 telnet 127.0.0.1 22 > /dev/null 2>&1 &
done

# Simulate DNS queries to multiple domains
for domain in google.com facebook.com github.com stackoverflow.com; do
    nslookup $domain > /dev/null &
done

# Simulate file transfer
dd if=/dev/zero bs=1024 count=100 2>/dev/null | nc -l 8080 &
sleep 1
timeout 2 nc 127.0.0.1 8080 < /dev/null > /dev/null 2>&1

wait
echo "Simulation completed."
EOF

chmod +x simulate_incident.sh
Subtask 7.2: Capture the Incident
Start capturing before running the simulation:

sudo tcpdump -i eth0 -w incident_capture.pcap -c 200 &
TCPDUMP_PID=$!

# Run the simulation
./simulate_incident.sh

# Stop capture
sleep 5
sudo kill $TCPDUMP_PID
Subtask 7.3: Analyze the Incident
Perform detailed analysis of the captured incident:

echo "=== Incident Analysis ==="

echo "1. Total packets captured:"
tcpdump -r incident_capture.pcap 2>/dev/null | wc -l

echo
echo "2. Connection attempts to SSH (port 22):"
tcpdump -r incident_capture.pcap port 22 -c 10

echo
echo "3. DNS resolution activities:"
tcpdump -r incident_capture.pcap port 53 -c 10

echo
echo "4. HTTP activities:"
tcpdump -r incident_capture.pcap port 80 -c 10

echo
echo "5. Unusual port activities:"
tcpdump -r incident_capture.pcap port 8080 -c 10
Task 8: Creating Custom Monitoring Solutions
Subtask 8.1: Build a Network Monitoring Dashboard Script
Create a comprehensive monitoring script:

cat > network_monitor.sh << 'EOF'
#!/bin/bash

INTERFACE="eth0"
LOG_DIR="$HOME/network_logs"
DATE=$(date +%Y%m%d_%H%M%S)

# Create log directory
mkdir -p "$LOG_DIR"

echo "Network Monitoring Dashboard"
echo "==========================="
echo "Interface: $INTERFACE"
echo "Log Directory: $LOG_DIR"
echo "Start Time: $(date)"
echo

# Function to monitor specific traffic types
monitor_traffic() {
    local traffic_type="$1"
    local filter="$2"
    local filename="$3"
    
    echo "Starting $traffic_type monitoring..."
    sudo tcpdump -i "$INTERFACE" "$filter" -w "$LOG_DIR/${filename}_${DATE}.pcap" -c 50 &
    echo "PID: $!"
}

# Start different monitoring sessions
monitor_traffic "HTTP Traffic" "port 80" "http"
monitor_traffic "HTTPS Traffic" "port 443" "https"
monitor_traffic "DNS Traffic" "port 53" "dns"
monitor_traffic "SSH Traffic" "port 22" "ssh"
monitor_traffic "ICMP Traffic" "icmp" "icmp"

echo
echo "All monitoring sessions started. Generating some traffic..."

# Generate test traffic
curl -s http://httpbin.org/get > /dev/null &
nslookup google.com > /dev/null &
ping -c 3 8.8.8.8 > /dev/null &

echo "Waiting for capture completion..."
wait

echo
echo "Monitoring completed. Files saved in $LOG_DIR"
ls -la "$LOG_DIR"
EOF

chmod +x network_monitor.sh
Subtask 8.2: Run the Monitoring Dashboard
Execute the monitoring script:

./network_monitor.sh
Subtask 8.3: Analyze the Results
Create a summary analysis of all captured files:

cat > analyze_all.sh << 'EOF'
#!/bin/bash

LOG_DIR="$HOME/network_logs"

echo "Network Traffic Analysis Summary"
echo "==============================="
echo "Analysis Date: $(date)"
echo

for pcap_file in "$LOG_DIR"/*.pcap; do
    if [ -f "$pcap_file" ]; then
        echo "File: $(basename "$pcap_file")"
        echo "Packets: $(tcpdump -r "$pcap_file" 2>/dev/null | wc -l)"
        echo "File Size: $(ls -lh "$pcap_file" | awk '{print $5}')"
        echo "---"
    fi
done

echo
echo "Top 5 most active protocols:"
for pcap_file in "$LOG_DIR"/*.pcap; do
    if [ -f "$pcap_file" ]; then
        tcpdump -r "$pcap_file" -n 2>/dev/null
    fi
done | grep -oE 'TCP|UDP|ICMP' | sort | uniq -c | sort -nr | head -5

EOF

chmod +x analyze_all.sh
./analyze_all.sh
Troubleshooting Common Issues
Permission Issues
If you encounter permission errors:

# Add your user to the appropriate group
sudo usermod -a -G wireshark $USER
# Or run with sudo
sudo tcpdump -i eth0
Interface Not Found
If your network interface is not found:

# List all interfaces
ip link show
# Use the correct interface name in your commands
No Packets Captured
If no packets are being captured:

# Check if interface is up
ip link show eth0
# Generate some traffic
ping -c 3 8.8.8.8
File Permission Issues
If you cannot read saved files:

# Change ownership of capture files
sudo chown $USER:$USER *.pcap
Conclusion
In this comprehensive lab, you have successfully learned how to use tcpdump for network monitoring and security analysis. You have accomplished the following key objectives:

Technical Skills Gained:

Installed and configured tcpdump on a Linux system
Captured network packets using various command-line options
Applied sophisticated filters to isolate specific types of traffic
Saved captured data to files for persistent analysis
Analyzed network traffic to identify potential security incidents
Security Analysis Capabilities:

Created custom monitoring solutions for different traffic types
Developed scripts for automated network monitoring
Learned to identify suspicious network patterns
Built comprehensive analysis workflows for incident response
Practical Applications:

Network troubleshooting and performance monitoring
Security incident detection and analysis
Compliance monitoring and audit trail creation
Forensic analysis of network communications
Why This Matters: Network monitoring with tcpdump is a fundamental skill for system administrators, security professionals, and network engineers. The ability to capture, filter, and analyze network traffic is essential for maintaining secure and efficient network operations. These skills are directly applicable to real-world scenarios including security incident response, network troubleshooting, and compliance monitoring.

The hands-on experience gained in this lab provides a solid foundation for advanced network security tools and prepares you for professional certifications in Linux security and network administration. The scripts and techniques learned here can be adapted and expanded for use in production environments, making this knowledge immediately practical and valuable.
